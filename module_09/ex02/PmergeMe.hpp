#pragma once

#ifndef PMERGEME_HPP 
#define PMERGEME_HPP 

#include <iostream>
#include <vector>
#include <deque>
#include <algorithm>

class PmergeMe
{
private:

public:	
	PmergeMe();
	// template <typename T> void insertion_sort(T& container);
	// template <typename T> void sort(T& container);
};

	template <typename T> void insertion_sort(T& container);
	template <typename T> void sort(T& container);
#endif //PMERGEME_HPP 